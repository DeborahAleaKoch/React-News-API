import { useState } from "react";
import { apiKey, ArticleResponse } from "./NewsSection";

const SearchButton = () => {
	return <></>;
	// <button
	// 	onClick={() => fetchData()}
	// 	className='border-1 border-slate-800 rounded-2xl px-4 py-2 hover:bg-slate-600 hover:text-slate-200 hover:border-slate-400 cursor-pointer'
	// >
	// 	Search
	// </button>
	// );
};

export default SearchButton;
